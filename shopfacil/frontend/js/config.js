// Configuración para Sprint 6 / despliegue en nube.
// En local se usa http://localhost:3000/api.
// En nube cambia el valor por la URL pública del backend de Render.
window.SHOPFACIL_API_URL = window.SHOPFACIL_API_URL || 'http://localhost:3000/api';
